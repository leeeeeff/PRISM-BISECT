# .streamlit/secrets.toml — copy to secrets.toml and fill in for production
# DO NOT commit secrets.toml (it is gitignored)

# Optional: contact email shown in Upload Mode
[app]
contact_email = "your@email.com"

# Optional: restrict upload mode to known reviewers
# Leave empty to allow all uploads
reviewer_token = ""
