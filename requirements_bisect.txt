# BISECT — Python dependencies (supplements requirements.txt)
# Install: pip install -r requirements_bisect.txt
# External tools also required: HMMER, TransDecoder, RepeatMasker, minimap2
# See README.md for external tool installation instructions.

# HTTP / API clients (STRING, AlphaFold, UCSC)
requests>=2.28.0

# Template rendering (M6 case reports)
Jinja2>=3.1.0

# Bioinformatics
biopython>=1.81

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
